from django.contrib import admin
# モデルをインポートし、管理画面に登録する場合は以下のようにします
# from .models import TellingRecord, TellingReminder
# 
# admin.site.register(TellingRecord)
# admin.site.register(TellingReminder)
